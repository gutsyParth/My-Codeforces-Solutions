t = int(input())

while t:
    t -= 1
    k = int(input())
    ans = 0
    while k:
        if (ans + 1) % 3 == 0 or (ans + 1) % 10 == 3:
            ans += 1
        else:
            ans += 1
            k -= 1

    print(ans)
